import { StockHistoryScreen } from '@/components/admin/screens/Inventory';

export const metadata = { title: 'Movement history' };

export default function Page() {
  return <StockHistoryScreen />;
}
