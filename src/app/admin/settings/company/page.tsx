import { CompanySettingsScreen } from '@/components/admin/screens/Settings';

export const metadata = { title: 'Company profile' };

export default function Page() {
  return <CompanySettingsScreen />;
}
