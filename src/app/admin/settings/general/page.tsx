import { GeneralSettingsScreen } from '@/components/admin/screens/Settings';

export const metadata = { title: 'General settings' };

export default function Page() {
  return <GeneralSettingsScreen />;
}
